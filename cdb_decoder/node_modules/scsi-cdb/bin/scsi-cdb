#!/usr/bin/env node

'use strict';

var argv = require('yargs').argv;

let ScsiCdb = require('../scsi-cdb');
let scsiCdb = new ScsiCdb();

if (argv.decode) {
	console.log('Looks like we\'re decoding:', argv.decode);
}